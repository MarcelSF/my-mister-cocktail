<div class="container my-5">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-8">
      <div class="card-cocktail-show">
        <div class="card-cocktail-top">
          <h1><%= @cocktail.name %></h1>
          <div class="image-cocktail mt-3">
            <%= cl_image_tag @cocktail.photo.key, height: 300, width: 400, crop: :fill %>
          </div>
        </div>
        <ul class="mt-3">
          <% if @cocktail.doses.any? %>
            <h5>Use these ingredients to make your <%= @cocktail.name %>:</h5>
            <% @cocktail.doses.each do |dose| %>
              <li><%= "#{dose.description}: #{dose.ingredient.name}" %></li>
            <% end %>
          <% else %>
            <h5>Hey there! Add some doses so other people can learn how to use your cocktail! 🥂 Use the form 😎</h5>
          <% end %>
        </ul>
      </div>
    </div>
    <div class="col-sm-12 col-md-4 col-lg-4 form-doses">
      <h5>Customize your <%= @cocktail.name %> with your own doses!</h3>
        <p>Here you can add any quantity from our pre-selected list of amazing ingredients 😁</p>
      <%= render 'doses/new', dose: @dose, cocktail: @cocktail %>
    </div>
  </div>
  <div class="row">


  </div>
</div>

