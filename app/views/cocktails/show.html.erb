<div class="container my-5">
  <div class="row p-3">
    <div class="col-sm-12 col-md-8 col-lg-8">
      <div class="card-cocktail-show">
        <div class="card-cocktail-top">
          <h1><%= @cocktail.name %></h1>
          <div class="image-cocktail mt-3">
            <%= cl_image_tag @cocktail.photo.key, height: 300, width: 400, crop: :fill %>
          </div>
        </div>
        <ul class="mt-3">
          <% if @cocktail.doses.any? %>
            <h4><bold>Use these ingredients to make your <%= @cocktail.name %>:</bold></h4>
            <% @cocktail.doses.each do |dose| %>
              <li><%= "#{dose.description}: #{dose.ingredient.name}" %></li>
            <% end %>
          <% else %>
            <h4>Hey there! Add some doses so other people can learn how to use your cocktail! 🥂 Use the form 😎</h4>
          <% end %>
        </ul>
      </div>
    </div>
    <div class="col-sm-12 col-md-4 col-lg-4 form-doses">
      <h5>Customize your <%= @cocktail.name %> with your own doses!</h3>
        <p>Here you can add any quantity from our pre-selected list of amazing ingredients 😁</p>
      <%= render 'doses/new', dose: @dose, cocktail: @cocktail %>
    </div>
  </div>
  <div class="row my-5">
    <div class="col-sm-12 col-lg-8 reviews-list">
      <% if  @cocktail.reviews.any?%>
        <h4>Here's what everyone thinks about this <%= @cocktail.name %>:</h4>
        <% @cocktail.reviews.each do |review|%>
          <div class="cocktail-review">
            <p>Score: <%=  "🍸 " * review.rating %></p>
            <p>Commentary: <%= review.content %></p>
          </div>
        <% end %>
      <% else %>
        <h4 class="p-3">No one has tasted this drink yet! Be the first one to leave a comment!</h4>
      <% end %>
    </div>
    <div class="col-sm-12 col-lg-4 reviews-form">
      <h5>What did you think of <%= @cocktail.name %>?</h5>
      <p>Leave a cool review to let people know how much you liked (or hated 😨) this cocktail</p>
      <%= render 'reviews/new', cocktail: @cocktail, review: @review %>
    </div>

  </div>
</div>

